<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div>
    <% if user_signed_in? %>
    |
    <%= link_to "Accoınt Settings", edit_user_registration_path %>
    <%= link_to 'Sign Out', destroy_user_session_path,  data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %> |
    <%else%>
    <%= link_to "Sing In", new_user_session_path%> |
    <%= link_to "Sing Up", new_user_registration_path%> |
    <%end%>
    </div>

    <%= yield %>
  </body>
</html>
